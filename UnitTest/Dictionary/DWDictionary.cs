namespace UnitTest.Dictionary
{
    public class DWDictionary
    {
        /*public  readonly Dictionary<string, Type> dwTypeDict;

        public DWDictionary(string tableName)
        {
            dwTypeDict = new Dictionary<string, Type>()
            {
                { "dwadm2.CD_ACCT", Assembly.GetExecutingAssembly().GetTypes().FirstOrDefault(t => t.Name == "dwadm2.CD_ACCT") },
                { "dwadm2.CD_ADDR", Assembly.GetExecutingAssembly().GetTypes().FirstOrDefault(t => t.Name == "dwadm2.CD_ADDR") },
                { "dwadm2.CD_DATE", Assembly.GetExecutingAssembly().GetTypes().FirstOrDefault(t => t.Name == "dwadm2.CD_DATE") },
                { "dwadm2.CD_FISCAL_CAL", Assembly.GetExecutingAssembly().GetTypes().FirstOrDefault(t => t.Name == "dwadm2.CD_FISCAL_CAL") },
                { "dwadm2.CD_PER", Assembly.GetExecutingAssembly().GetTypes().FirstOrDefault(t => t.Name == "dwadm2.CD_PER") },
                { "dwadm2.CD_PREM", Assembly.GetExecutingAssembly().GetTypes().FirstOrDefault(t => t.Name == "dwadm2.CD_PREM") },
                { "dwadm2.CD_RATE", Assembly.GetExecutingAssembly().GetTypes().FirstOrDefault(t => t.Name == "dwadm2.CD_RATE") },
                { "dwadm2.CD_SA", Assembly.GetExecutingAssembly().GetTypes().FirstOrDefault(t => t.Name == "dwadm2.CD_SA") },
                { "dwadm2.CD_SQI", Assembly.GetExecutingAssembly().GetTypes().FirstOrDefault(t => t.Name == "dwadm2.CD_SQI") },
                { "dwadm2.CD_UOM", Assembly.GetExecutingAssembly().GetTypes().FirstOrDefault(t => t.Name == "dwadm2.CD_UOM") },
            };
        }*/
    }
}
