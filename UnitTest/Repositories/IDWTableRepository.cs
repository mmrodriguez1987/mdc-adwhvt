namespace UnitTest.Repositories
{
    public interface IDWTableRepository
    {/*
        int GetRowCount(string tablename);

        List<DWTableContext> GetRowsCount(string tableName);
    */
    }
}
